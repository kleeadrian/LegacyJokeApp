{
  "name": "dadjoke-app",
  "version": "1.0.0",
  "description": "Sample dad joke application with Node.js frontend and backend",
  "private": true,
  "engines": {
    "node": ">=26.0.0"
  },
  "scripts": {
    "prestart": "node scripts/check-node-version.js",
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "start:backend": "npm start --prefix backend",
    "start:frontend": "npm start --prefix frontend",
    "start": "node scripts/start-all.js",
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "test": "npm test --prefix backend && npm test --prefix frontend",
    "lint": "npm run lint --prefix backend && npm run lint --prefix frontend"
  },
  "keywords": ["dadjoke", "nodejs", "sample-app"],
  "author": "",
  "license": "MIT"
}
